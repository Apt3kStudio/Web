@model Events
@{
    ViewData["Title"] = "UsersAcount";
}
@*@if (User.IsInRole("admin"))
    {*@
<div class="row">
    <div class="col-sm-12">
        <h2>Manage User</h2>
        <hr />
        <br />
    </div>
</div>


    <div class="row UserAccount-Row">       
        <div class="col-sm-8">
            <section>
                <form method="post" asp-action="PushEvent">                  
                    <div class="col-sm-8">
                        <select asp-for="EventName" class="form-control" asp-items="Model.PushEvents">
                            <option>Select User Type</option>
                        </select>
                    </div>
                    <div class="col-sm-4">
                        <button type="submit" class="btn btn-success">Save</button>
                    </div>
                </form>
            </section>
        </div>
    </div>
@*}*@